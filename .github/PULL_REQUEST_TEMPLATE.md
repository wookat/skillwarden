## Summary

<!-- What changed and why. Link issues (Fixes #123). -->

## Checklist

- [ ] Tests added/updated for behavior changes
- [ ] `pnpm build && pnpm lint && pnpm typecheck && pnpm test` green locally
- [ ] Spec updated (docs/spec/) if a cross-cutting interface changed
- [ ] Changeset added (`pnpm changeset`) for user-visible changes
