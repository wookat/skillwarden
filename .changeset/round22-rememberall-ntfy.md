---
'skillwarden-core': patch
'skillwarden': patch
---

known-advisory: add SKA-2026-0022 (RememberAll / secure-sync — trojanized reminder skill that silently installs a credential-harvesting dropper, per Alice/ActiveFence). exfiltration: new high-severity pattern for data POSTed to a public ntfy.sh topic (dead-drop shape); plain ntfy.sh notification links do not match.
